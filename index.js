// 터미널에 git init, chages 안에 있는건 변경사항을 표기해주는 것, q누르면 나가짐
import { Inter } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

export default function Home() {
  return (
    <div id="wrap"></div>;
}
